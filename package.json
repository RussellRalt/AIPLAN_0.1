{
  "name": "aiplan-app",
  "version": "1.0.0",
  "description": "Gestor de tareas con IA integrada",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "@google/generative-ai": "^0.11.3"
  },
  "engines": {
    "node": "18.x"
  }
}
